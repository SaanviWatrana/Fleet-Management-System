# Analytics & Reporting

Consumes historical operational events and telemetry to support decision-making. Candidate metrics include fleet utilization, mission completion, throughput, idle time, charging demand, fault frequency, mission latency, congestion patterns and resource utilization.

Analytics is intentionally downstream from real-time operational control paths.
